export function renderProfile(root, { user, preferences, onSave, onBack, onSignOut }) {
  root.innerHTML = `<main class="profile-shell"><header class="dashboard-header"><button class="brand brand-button" id="back">← chameleon<span>.</span></button><button class="button button-quiet" id="sign-out">Sign out</button></header><section class="profile-layout"><aside class="profile-aside"><div class="profile-avatar">${user.fullName.slice(0, 1).toUpperCase()}</div><h1>${user.fullName}</h1><p>${user.email}</p><span class="profile-tag">PERSONAL WEATHER SPACE</span></aside><section class="profile-card"><p class="eyebrow">PROFILE & PREFERENCES</p><h2>Make it feel like yours.</h2><form id="profile-form"><label>Home city<input name="homeCity" value="${preferences.homeCity}" required /></label><fieldset><legend>Temperature unit</legend><label class="choice"><input type="radio" name="unit" value="metric" ${preferences.unit === "metric" ? "checked" : ""} /> Celsius (°C)</label><label class="choice"><input type="radio" name="unit" value="imperial" ${preferences.unit === "imperial" ? "checked" : ""} /> Fahrenheit (°F)</label></fieldset><fieldset><legend>Appearance</legend><label class="choice"><input type="radio" name="theme" value="system" ${preferences.theme === "system" ? "checked" : ""} /> Day mode</label><label class="choice"><input type="radio" name="theme" value="dark" ${preferences.theme === "dark" ? "checked" : ""} /> Night mode</label></fieldset><button class="button button-primary" type="submit">Save changes <span>→</span></button></form></section></section></main>`;
  root.querySelector("#back").addEventListener("click", onBack);
  root.querySelector("#sign-out").addEventListener("click", onSignOut);
  root.querySelector("#profile-form").addEventListener("submit", (event) => { event.preventDefault(); onSave(Object.fromEntries(new FormData(event.currentTarget))); });
}
